apply {
    plugin 'groovy'
    plugin 'maven'
}

repositories { 
  	mavenCentral()
}

group = 'net.bosatsu.gradle'
version = '0.0.6-SNAPSHOT'

dependencies {
    compile gradleApi()
	// TODO: Investigate if we can use the Groovy that comes w/ NetKernel to avoid
	// version problems.
    groovy localGroovy()	
  	testCompile 'junit:junit:4.7'
}
